import * as chai from "chai";
import { default as chaiHttp, request } from "chai-http";
import sinon from "sinon";
chai.use(chaiHttp)

describe('Test Suite for Employee CRUD', () => {
    it('Should Verify getAllEmployees', () => {
        console.log('It for getAllEmployees')
    })
})

